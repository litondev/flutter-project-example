import 'package:flutter/material.dart';

class ForgotPassword extends StatelessWidget{
  Widget build(BuildContext context){
    return MaterialApp(
      home : Scaffold(
        body : Text("ForgotPassword")
      )
    );
  }
}