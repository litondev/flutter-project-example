import 'package:flutter/material.dart';

class ResetPassword extends StatelessWidget{
  Widget build(BuildContext context){
    return MaterialApp(
      home : Scaffold(
        body : Text("ResetPassword")
      )
    );
  }
}